#instructions for setting up folders and conda enviornment

##create a new cosmosis conda environment and folders
git clone https://github.com/icanswim/cosmosis.git
cd cosmosis
mkdir data
mkdir logs
mkdir models
conda env create -f environment.yml
conda activate cosmo
jupyter lab

##instructions for the manual creation of conda environment
git clone https://github.com/icanswim/cosmosis.git
cd cosmosis
mkdir data
mkdir logs
mkdir models
conda create --name cosmo -c pytorch -c nvidia python=3.10 pytorch torchvision torchaudio pytorch-cuda=11.7
conda activate cosmo
conda install -c conda-forge matplotlib
conda install -c conda-forge scipy pandas scikit-learn
conda install -c conda-forge jupyterlab h5py git
jupyter lab

##notes
1. scipy fails due to conflict with latest numpy
2. this build works with above exception
3. numerous conflict found within python and cuda versions resulting in runtime errors
